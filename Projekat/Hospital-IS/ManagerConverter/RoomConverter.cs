namespace Hospital_IS.ManagerConverter
{
    public class RoomConverter
    {
            /*public RoomViewModel ConvertModelToViewModel(Room room)
            {
                /*RoomViewModel roomViewModel = new RoomViewModel();

                roomViewModel.RoomNumber = room.RoomNumber.ToString();
                roomViewModel.RoomFloor = room.RoomFloor.ToString();
                roomViewModel.SurfaceArea = room.SurfaceArea.ToString();
                roomViewModel.BedNumber = room.BedNumber.ToString();
                roomViewModel.SelectedRoom= room;

                return roomViewModel;
            }

            public ObservableCollection<RoomViewModel> ConvertListToViewModel(List<Room> rooms)
            {
                ObservableCollection<RoomViewModel> viewModelRoooms = new ObservableCollection<RoomViewModel>();
                RoomViewModel roomViewModel;
                foreach (Room r in rooms)
                {
                    roomViewModel = ConvertModelToViewModel(r);
                    viewModelRoooms.Add(roomViewModel);
                }
                return viewModelRoooms;
            }*/
        }
}
